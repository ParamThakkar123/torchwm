"""Packaged YAML starter configs for TorchWM experiments.

This package is reserved for experiment configuration files (YAML).
Currently no YAML files are bundled here; configs are loaded from
user-provided paths or the library defaults in ``world_models.configs``.
"""
